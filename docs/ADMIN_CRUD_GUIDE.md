# Admin CRUD Operations Guide

## Overview
Цей документ описує всі доступні CRUD (Create, Read, Update, Delete) операції в адміністративній панелі для управління брендами, товарами та категоріями.

## 🎯 Brands Management (Управління брендами)

### ✅ Available Operations

#### 1. **Create Brand** (Створити бренд)
- **UI:** Кнопка "Add New Brand" на сторінці `/admin/brands`
- **API:** `POST /api/admin/brands`
- **Поля:**
  - `name` (required) - Назва бренду
  - `alias` (required, unique) - Унікальний ідентифікатор
  - `imageLink` (required) - URL зображення бренду
  - `isVisible` (optional, default: true) - Видимість бренду
  - `createdAt` (optional) - Дата створення

#### 2. **Read Brands** (Переглянути бренди)
- **UI:** Таблиця на сторінці `/admin/brands`
- **API:** `GET /api/admin/brands`
- **Features:**
  - Пагінація (5 брендів на сторінку)
  - Статистика: Total, Visible, Hidden, Linked Items
  - Відображення зображення бренду
  - Показ кількості пов'язаних товарів

#### 3. **Update Brand** (Оновити бренд)
- **UI:** Кнопка "Edit" для кожного бренду
- **API:** `PUT /api/admin/brands/{brandId}`
- **Features:**
  - Редагування всіх полів бренду
  - Зміна видимості
  - Автоматичне оновлення `updatedAt`

#### 4. **Delete Brand** (Видалити бренд)
- **UI:** Кнопка "Delete" для кожного бренду
- **API:** `DELETE /api/admin/brands/{brandId}`
- **Validation:**
  - ❌ Не можна видалити бренд, якщо до нього прив'язані товари
  - ✅ Показує помилку: "Cannot delete brand with associated items"

#### 5. **Toggle Visibility** (Змінити видимість)
- **UI:** Іконка Eye/EyeOff для кожного бренду
- **API:** `PUT /api/admin/brands/{brandId}`
- **Features:**
  - Швидке переключення видимості без відкриття модального вікна

---

## 📦 Items Management (Управління товарами)

### ✅ Available Operations

#### 1. **Create Item** (Створити товар)
- **UI:** Кнопка "Add New Item" на сторінці `/admin/items`
- **API:** `POST /api/admin/items`
- **Tabs:**
  - **Basic Info:**
    - `articleId` (required, unique) - Артикул товару
    - `categorySlug` (required) - Категорія або підкатегорія
    - `brandSlug` (optional) - Бренд
    - `isDisplayed` (default: false) - Відображення на сайті
    - `itemImageLink` (array) - Зображення товару
    - `warrantyType` (optional) - Тип гарантії
    - `warrantyLength` (optional) - Тривалість гарантії
  - **Item Details:**
    - Деталі товару для різних локалей (pl, uk, en)
    - `itemName`, `description`, `specifications`

#### 2. **Read Items** (Переглянути товари)
- **UI:** Таблиця на сторінці `/admin/items`
- **API:** `GET /api/admin/items`
- **Features:**
  - Пагінація (5 товарів на сторінку)
  - Статистика: Total Items, Displayed, Hidden
  - Відображення зображення, артикула, категорії, статусу
  - Bulk Upload та Export функції

#### 3. **Update Item** (Оновити товар)
- **UI:** Кнопка "Edit" для кожного товару
- **API:** `PUT /api/admin/items/{articleId}`
- **Features:**
  - Редагування основної інформації
  - Оновлення деталей товару
  - Управління цінами для різних складів
  - Зміна категорії/підкатегорії

#### 4. **Delete Item** (Видалити товар)
- **UI:** Кнопка "Delete" для кожного товару
- **API:** `DELETE /api/admin/items/{articleId}`
- **Warning:**
  - ⚠️ Видаляє всі пов'язані дані: ціни, деталі, історію цін

#### 5. **Toggle Display** (Змінити відображення)
- **UI:** Іконка Eye/EyeOff для кожного товару
- **API:** `PUT /api/admin/items/{articleId}`
- **Features:**
  - Швидке переключення відображення на сайті

#### 6. **Bulk Upload** (Масове завантаження)
- **UI:** Кнопка "Bulk Upload"
- **Features:**
  - Завантаження товарів через JSON/Excel
  - Масове створення товарів

#### 7. **Export Items** (Експорт товарів)
- **UI:** Dropdown "Download Items"
- **Formats:**
  - JSON
  - CSV

---

## 🗂️ Categories Management (Управління категоріями)

### ✅ Available Operations

#### 1. **Create Category** (Створити категорію)
- **UI:** Кнопка "Add New Category" на сторінці `/admin/categories`
- **API:** `POST /api/admin/categories`
- **Поля:**
  - `name` (required) - Назва категорії
  - `slug` (required, unique, auto-generated) - URL-friendly ідентифікатор
  - `subcategory` (array, optional) - Підкатегорії
    - `name` - Назва підкатегорії
    - `slug` - URL-friendly ідентифікатор
    - `isVisible` - Видимість підкатегорії
  - `isVisible` (optional, default: true) - Видимість категорії

#### 2. **Read Categories** (Переглянути категорії)
- **UI:** Таблиця на сторінці `/admin/categories`
- **API:** `GET /api/admin/categories`
- **Features:**
  - Пагінація (5 категорій на сторінку)
  - Статистика: Total, Visible, Hidden, With Subcategories
  - Відображення підкатегорій (перші 3 + решта)
  - Показ статусу видимості

#### 3. **Update Category** (Оновити категорію)
- **UI:** Кнопка "Edit" для кожної категорії
- **API:** `PUT /api/admin/categories/{slug}`
- **Features:**
  - Редагування назви та slug
  - Управління підкатегоріями (додавання/видалення)
  - Зміна видимості
  - **Important:** 🔄 При зміні slug автоматично оновлюються всі товари, що посилаються на цю категорію

#### 4. **Delete Category** (Видалити категорію)
- **UI:** Кнопка "Delete" для кожної категорії
- **API:** `DELETE /api/admin/categories/{slug}`
- **Validation:**
  - ❌ Не можна видалити категорію, якщо до неї прив'язані товари
  - ❌ Не можна видалити категорію, якщо до її підкатегорій прив'язані товари
  - ✅ Показує помилку з назвою підкатегорії, яка має товари

---

## 🔐 Security & Validation

### Authentication
- Всі API endpoints вимагають автентифікації
- Тільки користувачі з роллю `admin` мають доступ

### Validation Rules

#### Brands:
- `alias` повинен бути унікальним
- Неможливо видалити бренд з товарами

#### Items:
- `articleId` повинен бути унікальним
- `categorySlug` повинен існувати в базі даних
- Автоматична валідація чи це категорія або підкатегорія

#### Categories:
- `slug` повинен бути унікальним
- `slug` генерується автоматично з `name`
- Формат slug: lowercase, no special chars, hyphens only
- Неможливо видалити категорію з товарами
- При зміні slug оновлюються всі пов'язані товари

---

## 📊 Statistics & Monitoring

### Dashboard Metrics

#### Brands:
- Total Brands
- Visible Brands
- Hidden Brands
- Linked Items

#### Items:
- Total Items
- Displayed Items
- Hidden Items
- Percentage of displayed items

#### Categories:
- Total Categories
- Visible Categories
- Hidden Categories
- Categories with Subcategories

---

## 🚨 Error Handling

### Common Errors:

1. **401 Unauthorized** - Користувач не автентифікований
2. **403 Forbidden** - Користувач не має прав адміністратора
3. **404 Not Found** - Ресурс не знайдено
4. **400 Bad Request** - Некоректні дані або порушення валідації
5. **409 Conflict** - Конфлікт унікальності (наприклад, дублікат alias або slug)

### Specific Validation Messages:

**Brands:**
- "Brand with this alias already exists"
- "Cannot delete brand with associated items"

**Categories:**
- "Category with this slug already exists"
- "Cannot delete category with items. Please move or delete items first."
- "Cannot delete category. Subcategory '{name}' has items. Please move or delete items first."

**Items:**
- "Article ID is required"
- "Item not found"

---

## 🎨 UI Features

### Common Features:
- ✅ Responsive design
- ✅ Пагінація
- ✅ Search and filtering
- ✅ Inline editing
- ✅ Bulk operations (для items)
- ✅ Export/Import (для items)
- ✅ Real-time validation
- ✅ Success/Error notifications

### Modal Windows:
- **Create/Edit Modals** - Форми з валідацією
- **Delete Modals** - Підтвердження з попередженнями
- **Bulk Upload Modal** - Для масового завантаження items

---

## 🔧 Advanced Features

### Categories:
- **Auto-slug generation** - Slug генерується автоматично з назви
- **Subcategory management** - Додавання/видалення підкатегорій в реальному часі
- **Cascade updates** - При зміні slug категорії оновлюються всі пов'язані товари

### Items:
- **Multi-warehouse pricing** - Підтримка цін для різних складів
- **Price history** - Автоматичне зберігання історії цін
- **Multi-language support** - Деталі товару для різних мов
- **Image gallery** - Підтримка кількох зображень

### Brands:
- **Quick visibility toggle** - Швидка зміна видимості
- **Image preview** - Попередній перегляд зображення в списку

---

## 📝 Best Practices

1. **Before deleting:**
   - Перевірте чи немає пов'язаних товарів
   - Розгляньте можливість приховування замість видалення

2. **When updating categories:**
   - Будьте обережні при зміні slug - це впливає на URL
   - Перевірте що всі товари правильно прив'язані

3. **For items:**
   - Завжди додавайте деталі товару для всіх підтримуваних мов
   - Перевіряйте ціни для всіх складів
   - Використовуйте якісні зображення

4. **General:**
   - Використовуйте описові назви та aliases
   - Регулярно перевіряйте статистику
   - Використовуйте bulk operations для масових змін

---

## ⚠️ IMPORTANT NOTES

### Warehouses:
**НІ В ЯКОМУ РАЗІ НЕ ЧІПАТИ!** 
Управління складами (warehouses) має залишитися без змін і не підлягає модифікації в рамках цієї задачі.

### Database Integrity:
- Всі операції DELETE перевіряють foreign key constraints
- Cascade updates реалізовані для category slug changes
- Price history автоматично створюється при оновленні цін

---

## 🎯 Summary

Всі необхідні CRUD операції для управління брендами, товарами та категоріями **повністю реалізовані** та **функціонують коректно**:

✅ **Brands:** Create, Read, Update, Delete, Toggle Visibility
✅ **Items:** Create, Read, Update, Delete, Toggle Display, Bulk Upload, Export
✅ **Categories:** Create, Read, Update, Delete with subcategory management

Всі операції мають:
- ✅ Proper validation
- ✅ Error handling
- ✅ Security checks
- ✅ UI components
- ✅ API endpoints
- ✅ Database integrity checks
