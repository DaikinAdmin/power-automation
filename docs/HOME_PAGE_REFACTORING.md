# Рефакторинг головної сторінки

## Зміни

### 1. Створено новий хук `useCategories`
**Файл:** `src/hooks/useCategories.ts`

Хук відповідає за:
- Фетчинг категорій та субкатегорій з API
- Автоматичне оновлення при зміні locale
- Управління станом завантаження та помилок
- Можливість ручного перезавантаження даних

### 2. Розділено головну сторінку на компоненти

#### FeaturesSection (`src/components/home/features-section.tsx`)
- Відображає секцію з особливостями сервісу
- **Приховано на мобільних пристроях** (клас `hidden md:block`)

#### ProductsTabsSection (`src/components/home/products-tabs-section.tsx`)
- Табовий інтерфейс для відображення продуктів
- Включає логіку фільтрації: бестселлери, знижки, нові товари
- Інтегровано з `useCatalogPricing` та `useCart`

#### CategoriesSection (`src/components/home/categories-section.tsx`)
- **Мобільна версія:** 
  - Кнопка "Показати всі категорії" **над вкладками продуктів**
  - Категорії з'являються знизу у модальному вікні (slide-up animation)
- **Десктопна версія:**
  - Звичайна сітка категорій **під вкладками продуктів**
  - Відображення субкатегорій

### 3. Оновлено мобільний header

#### CategoriesAccordion (`src/components/layout/categories-accordion.tsx`)
- Акордеон для відображення категорій та субкатегорій
- Розгортається/згортається при кліку
- Автоматично закриває меню при переході на категорію

#### MobileHeader (`src/components/layout/mobile-header.tsx`)
- Інтегровано хук `useCategories`
- Додано `CategoriesAccordion` в гамбургер-меню
- Категорії відображаються першими в меню з можливістю розгортання

### 4. Tailwind конфігурація

Додано анімацію slide-up для мобільного модального вікна:
```typescript
keyframes: {
  "slide-up": {
    "0%": { transform: "translateY(100%)" },
    "100%": { transform: "translateY(0)" },
  },
},
animation: {
  "slide-up": "slide-up 0.3s ease-out",
},
```

## Структура компонентів

```
src/
├── hooks/
│   └── useCategories.ts          # Хук для фетчингу категорій
├── components/
│   ├── home/
│   │   ├── features-section.tsx       # Секція фічерів (приховано на mobile)
│   │   ├── products-tabs-section.tsx  # Секція продуктів з табами
│   │   └── categories-section.tsx     # Секція категорій (адаптивна)
│   └── layout/
│       ├── categories-accordion.tsx   # Акордеон категорій для mobile menu
│       └── mobile-header.tsx          # Оновлений mobile header
└── app/
    └── [locale]/
        └── page.tsx                   # Спрощена головна сторінка
```

## Мобільна адаптація

### Фічери
- ❌ Приховані на мобільних пристроях

### Категорії
- 📱 **Mobile:** Кнопка над табами → модальне вікно знизу
- 🖥️ **Desktop:** Сітка категорій під табами

### Навігація
- 📱 **Mobile:** Акордеон категорій в гамбургер-меню
- 🖥️ **Desktop:** Залишається без змін

## Переваги рефакторингу

1. ✅ **Краща підтримуваність** - код розділений на логічні компоненти
2. ✅ **Переусування даних** - категорії завантажуються один раз через хук
3. ✅ **Мобільна оптимізація** - адаптивний дизайн для різних пристроїв
4. ✅ **Краща UX на mobile** - зручна навігація з категоріями
5. ✅ **DRY принцип** - немає дублювання логіки фетчингу категорій
