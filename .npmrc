# Use hoisted node_modules structure for better compatibility
node-linker=hoisted

# Preserve symlinks for proper module resolution
symlinks=true

# Shamefully hoist for better compatibility with Docker
shamefully-hoist=true

# Public hoist pattern to ensure better-auth and next can find each other
public-hoist-pattern[]=*
